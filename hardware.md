# hardware.md — Gemini PDA Hardware Inventory

Device: Planet Computers Gemini PDA  
SoC: MediaTek MT6797X (Helio X27)  
Vendor kernel: 3.18 (k97v1_64_bsp)  
DTB source: extracted from `kali_boot.img` (vendor boot image, `boot2` partition)

Reference projects: Kali Linux (Re4son, kernel 3.18), Gemian (kernel 3.18), Jasu buildroot (mainline attempt), postmarketOS (mainline attempt)

---

## Hardware Inventory

| Subsystem | Hardware / Chip | Function | Vendor Driver / Path | Device Tree Node | Mainline Status | Required Tier | Action | Reference Implementation | Notes / Evidence |
|-----------|----------------|----------|---------------------|-----------------|----------------|--------------|--------|--------------------------|-----------------|
| **CPU** | MT6797X — 2× Cortex-A72 @2.6 GHz + 4× Cortex-A53 @2.0 GHz + 4× Cortex-A53 @1.6 GHz | Application processor (10-core big.LITTLE) | MTK kernel / PSCI | `compatible = "arm,cortex-a72"` / `"arm,cortex-a53"` / `"arm,psci-0.2"` | Upstreamed | Boot Critical | Use Upstream | Kali/Gemian — Android/MTK vendor driver — custom MTK HMP scheduler + `arch/arm64/mach-mediatek/` BSP glue in 3.18; Jasu buildroot uses mainline `mt6797.dtsi` with upstream PSCI | `arch/arm64/boot/dts/mediatek/mt6797.dtsi` exists in mainline. PSCI supported. Mainline drops HMP scheduler; standard EAS used instead. |
| **Interrupt Controller** | ARM GIC-v3 | Interrupt routing | `irqchip/irq-gic-v3.c` | `gic@10240000` | Upstreamed | Boot Critical | Use Upstream | Kali/Gemian — Upstream Linux driver — standard `irq-gic-v3.c` used unchanged in vendor 3.18 | Standard ARM GIC-v3; fully supported in all kernels. No porting needed. |
| **Timers** | ARM Generic Timer (ARMv8) | System timer, scheduler clock | `clocksource/arm_arch_timer.c` | `compatible = "arm,armv8-timer"` | Upstreamed | Boot Critical | Use Upstream | Kali/Gemian — Upstream Linux driver — standard `arm_arch_timer.c` used unchanged in vendor 3.18 | Standard ARMv8 timer. No porting needed. |
| **Clocks** | MT6797 TOPCKGEN / APMIXEDSYS / INFRACFG | PLL, clock gating, dividers | `drivers/clk/mediatek/` | `compatible = "mediatek,topckgen"` / `"mediatek,apmixed"` | Upstreamed | Boot Critical | Use Upstream | Kali/Gemian — Android/MTK vendor driver — custom MTK clock framework in `drivers/clk/mediatek/` (3.18 BSP, not mainline API-compatible); mainline `clk-mt6797-*.c` is a clean rewrite | `clk-mt6797-*.c` drivers in mainline. All major clocks covered. Mainline clock driver is a separate implementation from the 3.18 vendor one — do not port from 3.18, use mainline directly. |
| **Pin Control / GPIO** | MT6797 pinctrl | GPIO and pin-mux control | `drivers/pinctrl/mediatek/` | `compatible = "mediatek,aeon_gpio"` | Upstreamed | Boot Critical | Use Upstream | Kali/Gemian — Android/MTK vendor driver — `pinctrl-mt6797.c` in 3.18 is a vendor BSP file; mainline `pinctrl-mt6797.c` is a separate upstreamed rewrite | `pinctrl-mt6797.c` in mainline. Vendor DTS uses `mediatek,aeon_gpio`; mainline binding differs — DTS node names will need updating. |
| **PMIC** | MT6351 | Power management IC — regulators, RTC, fuel gauge | vendor `drivers/misc/mediatek/pmic/` | `compatible = "mediatek,mt6351-pmic"` — via PMIC wrapper | **Not Upstreamed** (CORRECTED 2026-06-10) | Boot Critical | Port Driver (deferred) / fixed-regulator stubs for bring-up | Kali/Gemian — Android/MTK vendor driver — custom MTK PMIC framework (`drivers/misc/mediatek/pmic/`) replacing standard regulator API | **CORRECTION (2026-06-10, verified against v6.6 tree):** no `mt6351` MFD, regulator, or RTC driver exists in mainline — only the ASoC audio codec (`sound/soc/codecs/mt6351.c`). Prior "Upstreamed" claim was wrong (mt6357/58/59 exist; 6351 never landed). Bring-up path: LK leaves rails configured; use `regulator-fixed` stubs (as already done for vproc). Real driver = new `mt6397`-family MFD cell + regulator driver port, post-freeze. PWRAP itself **is** mainlined. |
| **PMIC Wrapper** | MediaTek PMIC Wrapper (PWRAP) | SPI-like bus to MT6351 | `drivers/soc/mediatek/mtk-pmic-wrap.c` | `pwrap@1000d000` | Upstreamed | Boot Critical | Use Upstream | Kali/Gemian — Android/MTK vendor driver — vendor `pwrap` driver in 3.18 BSP; mainline `mtk-pmic-wrap.c` covers MT6797 | Required for any PMIC access. Mainlined. |
| **Regulators (external)** | Richtek RT5735 (vproc/vgpu) | CPU/GPU DVFS buck converters | vendor `rt,rt5735-regulator` | `compatible = "rt,rt5735-regulator"` | Not Upstreamed | Boot Critical | Port Driver | Kali/Gemian — Android/MTK vendor driver — `drivers/misc/mediatek/power/mt6797/rt5735.c`; no known downstream Linux port | RT5735 not in mainline. Required for CPU voltage scaling; can boot at fixed voltage to defer. Plan: add as new variant in `fan53555.c`. See [driver_ports.md — RT5735](driver_ports.md#rt5735-buck-regulator). |
| **eMMC / MMC Controller** | MediaTek MSDC (mt6797) | 64 GB eMMC internal storage + microSD | `drivers/mmc/host/mtk-sd.c` | `msdc0@11230000` / `compatible = "mediatek,mt6797-mmc"` | Upstreamed | Boot Critical | Use Upstream | Kali/Gemian — Android/MTK vendor driver — custom MSDC driver in `drivers/mmc/host/mt_sd.c` (3.18 BSP); mainline `mtk-sd.c` is a clean rewrite with MT6797 binding | MT6797 binding exists in mainline. Mature driver. Do not port from vendor — use mainline `mtk-sd.c`. |
| **UART** | MT6797 UART (4× channels) | Serial console, debug | `drivers/tty/serial/8250/8250_mtk.c` | `compatible = "mediatek,mt6797-uart"` — `ttyMT0` at 921600 | Upstreamed | Boot Critical | Use Upstream | Kali/Gemian — Android/MTK vendor driver — custom MTK UART in `drivers/tty/serial/mtk_uart.c` (3.18); mainline uses `8250_mtk.c` with `mediatek,mt6797-uart` binding | Console on `ttyMT0`. MT6797 UART binding in mainline `8250_mtk.c`. Primary debug interface. |
| **GPU** | ARM Mali T860 MP4 @875 MHz | Graphics | Panfrost (`drivers/gpu/drm/panfrost/`) | `compatible = "arm,malit860", "arm,mali-t86x", "arm,mali-midgard"` | Upstreamed | Optional | Use Upstream | Kali/Gemian — Android/MTK vendor driver — proprietary ARM Mali Midgard DDK (`drivers/gpu/arm/midgard/`) linked against Android gralloc; postmarketOS notes Panfrost works on mainline | Panfrost supports Mali T860 (Midgard). Available kernel 5.2+. Defer until display works. Panfrost is a clean break from the proprietary DDK — no porting needed. |
| **Display Controller** | MediaTek MMSYS / DDP | Display Data Path — OVL, RDMA, DSI | `drivers/gpu/drm/mediatek/` | `mediatek,disp_ovl0`, `mediatek,disp_rdma0`, `mediatek,disp_dsi0` | Partial | Usability Critical | Port Driver | Kali/Gemian — Android/MTK vendor driver — `drivers/misc/mediatek/video/mt6797/` DDP driver tied to Android SurfaceFlinger; Jasu buildroot reports no display on mainline | Port Driver — **code complete, compiles**. MT6797 DDP path + mmsys_driver_data added to `mtk_drm_drv.c`; MT6797 DSI driver data added to `mtk_dsi.c`; MT6797 MIPITX PHY (`phy-mtk-mipi-dsi-mt6797.c`) written with correct PLL at 0x0050, BG at 0x0044; display pipeline DTS nodes added to `mt6797.dtsi` (OVL0, OVL_2L0, RDMA0, COLOR, CCORR, AAL, GAMMA, OD, DITHER, MUTEX, DSI0, MIPI_TX0). Note: dual-DSI not yet implemented. See [driver_ports.md](driver_ports.md#mt6797-display-controller). |
| **Display Panel** | Solomon SSD2092 — FHD MIPI DSI video mode, 1080×2160 | 1080×2160 MIPI DSI display panel | MTK LCM framework (vendor only) | Confirmed by direct DCS interrogation on real hardware (Phase 5, 2026-07-10, boot.md) | Not Upstreamed | Usability Critical | Port Driver | `panel-solomon-ssd2092.c`, `patches/v6.6/panel/0005-...` — active Phase 5 driver, DSI video mode, DRM-bound and producing pixels on real hardware | Port Driver — **in active use, functional** (backlight/pixels confirmed on hardware; a fade-to-black issue is mitigated with a userspace workaround, and a thin-line timing artifact is under investigation — see boot.md). **Correction (2026-07-11):** this row previously named the panel "Renesas R63419 — WQHD (1440×2560), CMD mode," based on `atag,videolfb-lcmname = "r63419_wqhd_truly_phantom_2k_cmd_ok"` in the real device's extracted DTB and matching entries in both public vendor kernel source trees (`dguidipc/gemini-android-kernel-3.18` and its `gemian` fork). **The user has directly confirmed the real physical Gemini panel is 1080×2160, not WQHD** — so that vendor-DTB/source entry describes a different hardware/panel revision, not this actual unit. Note "SSD2092"/"solomon_touch" in every vendor source found is actually the *touchscreen* controller's name, not the display's — the SSD2092 *panel* identity used here was established independently via direct hardware interrogation on this device, not from any vendor source tree. Do not revert to R63419/CMD-mode based on the vendor DTB alone. |
| **Touchscreen** | **Solomon SSD2092 (hardware-confirmed 2026-07-16)** — i2c4 addr 0x53, RST=GPIO68, INT=GPIO85 (EINT, B-11) | Capacitive multi-touch | Vendor SSL SSD20xx driver (gemian tree only; archived `docs/vendor-touch-ssd20xx/`) | `solomon_touch@53` on i2c@11011000 in vendor DTB | Not Upstreamed | Usability Critical | Port Driver | Gemian — `drivers/input/touchscreen/mediatek/SSD209XX/` (`CONFIG_TOUCHSCREEN_MTK_SSL_SSD20XX`), present ONLY in the gemian 3.18 tree, not dguidipc's | **Chip identity RESOLVED on hardware 2026-07-16 (build #263, boot.md):** i2cdetect with reset (GPIO68) driven high shows 0x53 ACK / 0x62 absent → Solomon SSD2092, NOT Novatek (Novatek = WQHD/R63419 variant only). Driver's embedded FW header says "Product ID AUO599, IC Name SSD2092" — matches our 5.99" AUO panel. LK leaves RST low (same trap as the keyboard). No mainline driver exists; Stage B = port the vendor driver with polled INT (GPIO85 EINT unusable until B-11). Source archived: `docs/vendor-touch-ssd20xx/`. |
| **Keyboard Matrix Controller** | AWINIC AW9523B (GPIO expander / LED driver, I2C `0x58`) | Physical QWERTY keyboard matrix | Vendor AW9523 driver | `aw9523_key` / `compatible = "mediatek,aw9523_key"` | Not Upstreamed | Usability Critical | Port Driver | Gemian — Downstream Linux driver — Gemian ships a custom keyboard input driver for Linux using AW9523B; key mappings in `symbols/planet_vndr/gemini`; Kali uses same Gemian-derived driver | CORRECTION: AW9523B not in mainline Linux 6.6 (no `aw9523` entries in `drivers/gpio/`; prior claim "mainlined ~6.0" is incorrect). `matrix_keypad.c` exists and uses standard gpiolib consumer API — could work if AW9523B GPIO driver is written. Two paths: (A) write AW9523B GPIO driver + use `matrix-keypad` (clean, upstreamable); (B) port Gemian custom keyboard driver directly (Linux input driver, most direct path). See [driver_ports.md](driver_ports.md#aw9523b-keyboard). |
| **Keyboard Backlight** | AWINIC AW9120 (LED controller, I2C `0x2c`) | Keyboard LED backlight | Vendor AW9120 LED driver | `aw9120_led@2c` | Not Upstreamed | Optional | Port Driver | Kali/Gemian — Android/MTK vendor driver — vendor AW9120 LED driver; not ported to Linux LED subsystem | AW9120 not in mainline. Can be deferred; keyboard function does not depend on backlight. |
| **USB (LEFT port)** | MediaTek MTU3 (mtu3 + xhci-mtk, USB2 only wired) at `0x11271000`, U2 PHY at `0x11290800` | LEFT USB-C port — peripheral/gadget role + BQ25896 charging input | `drivers/usb/mtu3/`, `drivers/phy/mediatek/` | `ssusb: usb@11271000`, `compatible = "mediatek,mtu3"`; `dr_mode = "peripheral"` (dts/0015, current) | Upstreamed | Boot Critical (charging) / Usability Critical (gadget) | Use Upstream | Kali/Gemian — Android/MTK vendor driver — custom MTK SSUSB (`drivers/misc/mediatek/usb20/`, 3.18); mainline `mtu3`/`xhci-mtk` is a clean rewrite | **Working (build #255, B-22 resolved 2026-07-16).** This PHY has no hardware VBUS/session sensing (B-20) — `u2port0` needs `mediatek,force-b-session-valid` (phy/0001) forced in software or the gadget never enumerates. A B-19 experiment temporarily forced this port into host mode (`dr_mode="otg"` + `role-switch-default-mode="host"` + `xhci` child, `vbus-supply=<&otg_vbus>`) to test USB-ethernet here; that config auto-enabled the BQ25896 OTG boost at every boot and silently blocked charging, so it was retired (`dts/0015`) once host duty moved to the right port. Do not re-add host-mode DTS here without also re-solving that charging conflict. |
| **USB (RIGHT port)** | Legacy Mentor Graphics MUSB IP ("musbfsh" in vendor source) at `0x11200000`, U2 PHY (tphy-v1) at `0x11210800` | RIGHT USB-C port — host-only, USB ethernet dongles | `drivers/usb/musb/mediatek.c` + generic MUSB core | `usb1: usb@11200000`, `compatible = "mediatek,mt6797-musb"` (dts/0014); `dr_mode = "host"` | Upstreamed (glue needs a Gemini-specific hardware config) | Usability Critical | Use Upstream + local config variant | Kali/Gemian — Android/MTK vendor driver — `drivers/misc/mediatek/usb11/mt6797/musbfsh_core.c`; mainline `drivers/usb/musb/mediatek.c` glue was written for MT2701/MT8516's OTG-capable MUSB, not MT6797's cut-down host-only musbfsh | **Working (build #255, B-22 resolved 2026-07-16).** Fully independent controller from the left port (xhci-mtk/mtu3) — separate register banks, no shared FIFO/arbitration/settings; the two ports must be diagnosed separately (blockers.md B-22 "why the two ports must be treated separately"). Fixes needed beyond generic MUSB: Gemini-specific `musb_hdrc_config` selected via `of_device_get_match_data()` — `num_eps=6` + a trimmed EP1-5 512B single-buffered FIFO table matching the vendor `musbfsh_config_mt65xx`, `multipoint` left at mainline's `true` default (forcing it `false` regressed enumeration itself, build #253 vs #254 — see `usb/0002` and boot.md 2026-07-16). The interim `maximum-speed = "full-speed"` cap (builds #249–#251's babble theory) was **retired 2026-07-20 by `dts/0019`**: the real bulk defect was the FIFO/num_eps mismatch, and high-speed is stable with well-behaved devices (SZNX cdc_ether, >200 MB iperf3 clean; the babbling RTL8156 was device-specific). Residual state (B-22 follow-up): PIO-only + single-buffered FIFOs cap TCP throughput at ~43–64 Mbit/s (RX overruns), and a babbling device wedges the host until reboot — throughput headroom + babble recovery for display-grade sustained bulk is tracker issue #27 (Slice U2). `u2port1` needs `mediatek,force-usb-host` (phy/0001) since this PHY also has no hardware VBUS/session sensing. |
| **USB-C CC Controller** | ON Semi FUSB301A — TWO chips: right-port/HDMI-mux (I2C0 `0x25`), left-port (I2C1 `0x25`, found 2026-07-14) | USB Type-C CC logic, VBUS control | Vendor FUSB301 driver | `fusb301a@25` on i2c0 (right/HDMI, disabled) and i2c1 (left, `okay`) / `compatible = "onsemi,fusb301a"` | Not Upstreamed | Usability Critical | Port Driver | Kali/Gemian — Android/MTK vendor driver — vendor FUSB301/FUSB301A driver from ON Semi Android BSP; no known downstream Linux port | FUSB301A not in mainline. FUSB302 exists (`drivers/usb/typec/tcpm/fusb302.c`) but is a full TCPM — different chip architecture, not adaptable for FUSB301A. FUSB301A is CC-only (presence/orientation detection; no Power Delivery). Both ports currently run fixed-role (left=peripheral, right=host) without needing live CC detection — driver (`patches/v6.6/usb/0001`) programs static SOURCE and polls, no IRQ available until B-11 (EINT) is fixed. See [driver_ports.md](driver_ports.md#fusb301a-cc-controller). |
| **Wi-Fi / Bluetooth / GPS** | MediaTek CONSYS (integrated in MT6797X SoC) | 802.11ac Wi-Fi, BT 4.1, A-GPS | Vendor CONSYS driver | `consys@18070000` / `compatible = "mediatek,mt6797-consys"` | Not Upstreamed | Usability Critical | Port Driver (deferred — Phase 9) | Kali (Re4son) — Android/MTK vendor driver with downstream patches — `drivers/misc/mediatek/connectivity/` CONSYS driver + `wlan_drv_gen2` Wi-Fi driver; Re4son adds monitor mode / injection patches on top; Gemian uses same driver stack; no mainline port known | **Architecture confirmed (2026-06-08 research):** CONSYS is an AHB-mapped hard-IP block — NOT a discrete SDIO chip. WiFi uses AHB bus (not SDIO); BT uses BTIF (on-chip UART) via STP multiplexer. conninfra is NOT applicable (MT7921-era Filogic architecture only). btmtksdio does NOT support MT6625 (device table covers MT7663/MT7668/MT7921/MT7902 only). mt76 is PCIe/USB-only. Zero mainline coverage. Last known working out-of-tree: kernel 5.6 (frank-w BPI-Router-Linux). Broken on 6.0+ with unidentified API changes. Driver requires userspace daemon (`wmt_launcher`) + firmware blobs from `/system/etc/firmware/`. Full driver stack: ~75–103 KLOC (gen2 path). See [driver_ports.md — CONSYS WiFi/BT](driver_ports.md#mt6797-consys-wifibt) and [research.md](research.md#mt6797-consys-wifi--bluetooth--gps--deep-research). USB-Ethernet (USB dongle via `xhci-mtk`) is the Phase 8 networking solution. WiFi/BT port is Phase 9 optional. |
| **Battery Charger** | TI BQ25896 (I2C bus 0, `0x6b`) — **verified live 2026-07-14** (REG14=0x06 → PN=000 rev 2). The earlier "Richtek RT9466 @ 0x53" identification was **wrong** — it came from a dead vendor config branch; no I2C bus has anything at 0x53 (see research.md §8) | Li-ion battery charger, input from the LEFT USB-C port; boost regulator (unused in the current build) | Vendor `bq25890.c` + `charging_hw_bq25890.c` (`CONFIG_MTK_BQ25896_SUPPORT`); the vendor DTB has NO charger node (driven without DT) | none | Upstreamed | Boot Critical | Use Upstream | Mainline `drivers/power/supply/bq25890_charger.c`, compatible `ti,bq25896` | **Working (build #255, B-22 resolved 2026-07-16):** `bq25890-charger-0` reports `Charging`/`online=1` with a charger in the left port. The driver also exposes the chip's OTG boost as a `usb-otg-vbus` regulator; an earlier experiment (B-19) wired that regulator as the left port's `vbus-supply` to test host mode there, which auto-enabled the boost at every boot and blocked charging input — that DTS wiring is retired (see the USB left-port row above), so the regulator node exists but is currently unreferenced. Boost is gated by BOTH REG03 OTG_CONFIG **and** GPIO107 (`GPIO_OTG_DRVVBUS_PIN`, LK leaves it low; fails silently when low) if it's ever wired up again. The chip's 40s I2C watchdog resets registers to defaults unless disabled (REG07[5:4]=00) or kicked. No IRQ: INT pin is on an MTK EINT (B-11); driver patched IRQ-optional (`power/0001`), so charge-state changes are only noticed on `power_supply` polls. |
| **Fuel Gauge / Battery** | MT6351 integrated (via PMIC wrapper) | Battery state-of-charge, voltage/current | Vendor MT6351 battery meter | `compatible = "mediatek,battery"` / `"mediatek,bat_meter"` | Partial | Usability Critical | Research Further | Kali/Gemian — Android/MTK vendor driver — `drivers/misc/mediatek/battery/` MTK battery meter, tightly coupled to Android power HAL | No mainline MT6351 fuel gauge or ADC driver. `mt6360-adc.c` and `mt6370-adc.c` exist for different PMICs; MT6351 ADC not mainlined. `generic-adc-battery.c` exists but needs an IIO ADC input not available for MT6351. Use charger-only mode (RT9467 driver) initially. Minimum safe operation: charger-only + userspace voltage monitor. Full SoC reporting requires new `mt6351-adc.c` driver (~200–500 LOC). Defer fuel gauge to Phase 7 post-charger bring-up. |
| **LTE Modem** | Integrated MT6797 baseband (md1) — Cat-6 LTE | 4G LTE, DC-HSPA+, GSM | Vendor CCCI/RILD framework | `md1img`, `md1dsp`, `md1arm7` partitions; `mediatek,ap_cldma` | Not Upstreamed | Optional | Defer | Kali/Gemian — Android/MTK vendor driver — full CCCI framework (`drivers/misc/mediatek/ccci_util/`, `drivers/misc/mediatek/cldma/`) + proprietary modem firmware; not functional in Gemian Linux desktop mode | Proprietary modem subsystem. CCCI is entirely vendor. Not feasible to upstream. Defer indefinitely. |
| **C2K Modem** | MT6297 (md3img) | CDMA2000 / EV-DO | Vendor CCCI | `md3img` partition; `mediatek,ap2c2k_ccif` | Not Upstreamed | Optional | Defer | Kali/Gemian — Android/MTK vendor driver — vendor C2K CCCI; not used in Linux configurations | Second modem for CDMA markets. Defer indefinitely. |
| **Audio Codec** | MT6351 integrated audio (AFE + codec) | Speaker, microphone, headphone | `sound/soc/mediatek/mt6797/`, `sound/soc/codecs/mt6351.c` | `compatible = "mediatek,audio"` | Upstreamed | Optional | Use Upstream | Kali/Gemian — Android/MTK vendor driver — `sound/soc/mediatek/mt6797/` vendor AFE driver tied to Android AudioFlinger; mainline ASoC driver is a clean rewrite | `CONFIG_SND_SOC_MT6797_MT6351` — ASoC driver in mainline (4.18+). Defer until after boot stabilisation. |
| **HDMI Output** | Silicon Image SII9022A (I2C) | HDMI transmitter (via USB-C alt mode or dedicated port) | Vendor SII9022 driver | `compatible = "mediatek,sii9022_hdmi"` | Upstreamed | Optional | Use Upstream | Kali/Gemian — Android/MTK vendor driver — vendor SII9022 driver; `drivers/gpu/drm/bridge/sii902x.c` is an independent mainline implementation | `drivers/gpu/drm/bridge/sii902x.c` in mainline. HDMI bridge driver available. Defer to Phase 5+. |
| **Fingerprint Sensor** | FPC1145 (SPI, `compatible = "fpc,fpc1020"`) | Fingerprint authentication | Vendor FPC driver | `fpc1145@0` | Not Upstreamed | Optional | Defer | Kali/Gemian — Android/MTK vendor driver — FPC Android fingerprint HAL driver; not used in Linux | FPC1145 not in mainline. Low priority. |
| **Accelerometer** | Bosch BMI160 + ST LSM6DS3 (I2C) | 3-axis acceleration | Vendor MTK sensor hub | `mediatek,bmi160_acc` / `mediatek,lsm6ds3_acc` | Upstreamed | Optional | Use Upstream | Kali/Gemian — Android/MTK vendor driver — MTK sensor hub abstraction (`drivers/misc/mediatek/sensor_hub/`) wrapping vendor BMI160/LSM6DS3 drivers; crashes on boot in Linux mode without `initcall_blacklist` | BMI160: `drivers/iio/imu/bmi160/`. LSM6DS3: `drivers/iio/imu/st_lsm6dsx/`. Both mainlined. Bypass vendor sensor hub entirely — use IIO drivers directly with standard DTS bindings. |
| **Gyroscope** | Bosch BMI160 + ST LSM6DS3 (I2C) | 3-axis angular velocity | Vendor MTK sensor hub | `mediatek,bmi160_gyro` / `mediatek,lsm6ds3_gyro` | Upstreamed | Optional | Use Upstream | Kali/Gemian — Android/MTK vendor driver — same MTK sensor hub as accelerometer; same crash behaviour | Same IIO drivers as accelerometer (combo chip). |
| **Magnetometer** | MEMSIC MMC3530 (I2C `0x30`) | 3-axis compass | Vendor MTK sensor hub | `msensor_mmc3530@30` | Not Upstreamed | Optional | Defer | Kali/Gemian — Android/MTK vendor driver — MTK sensor hub MMC3530 driver; no known IIO port | MMC3530 not in mainline IIO. MMC5603 and newer MEMSIC parts are mainlined; MMC3530 is not. |
| **Barometer** | Bosch BMP280 (I2C) | Atmospheric pressure | Vendor MTK sensor hub | `compatible = "mediatek,bmp280"` | Upstreamed | Optional | Use Upstream | Kali/Gemian — Android/MTK vendor driver — MTK sensor hub BMP280 wrapper; mainline IIO BMP280 driver is independent | `drivers/iio/pressure/bmp280.c` in mainline. Needs standard I2C binding (`bosch,bmp280`). |
| **Ambient Light / Proximity** | SensorTek STK3x1x (I2C) | ALS + proximity sensing | Vendor `stk3x1x_local_init` | `compatible = "mediatek,stk3x1x"` | Not Upstreamed | Optional | Defer | Kali/Gemian — Android/MTK vendor driver — STK3x1x vendor driver; this driver caused the boot crash loop in prior work when running in Linux mode (I2C probe before hardware ready) | STK3x1x vendor driver. Not mainlined. Confirm disabled in new kernel config — this was the root cause of the prior crash loop. |
| **Humidity Sensor** | ST HTS221 (I2C) | Relative humidity + temperature | Vendor MTK sensor hub | `compatible = "mediatek,hts221"` | Upstreamed | Optional | Use Upstream | Kali/Gemian — Android/MTK vendor driver — MTK sensor hub HTS221 wrapper; mainline IIO HTS221 driver is independent | `drivers/iio/humidity/hts221.c` in mainline. |
| **Sensor Control Processor** | MediaTek SCP (Cortex-M4 coprocessor) | Sensor hub offload | `drivers/remoteproc/` (partial) | `scp@10020000` | Partial | Optional | Defer | Kali/Gemian — Android/MTK vendor driver — custom SCP firmware + driver in `drivers/misc/mediatek/scp/`; not used in any mainline port | SCP remoteproc framework partially mainlined for newer MTK SoCs. MT6797 SCP support status unclear. Defer until sensors needed. |
| **Thermal** | MT6797 thermal controller | CPU thermal monitoring, throttling | `drivers/thermal/mediatek/mtk-thermal.c` | `compatible = "mediatek,thermal"` | Upstreamed | Boot Critical | Use Upstream | Kali/Gemian — Android/MTK vendor driver — custom MTK thermal in `drivers/misc/mediatek/thermal/`; mainline `mtk-thermal.c` is a clean rewrite | MTK thermal driver mainlined. Required to prevent thermal runaway on bring-up. Do not port from 3.18 vendor driver. |
| **CPUFreq / DVFS** | MT6797 CPUFreq + RT5735 regulators | Dynamic voltage/frequency scaling | `drivers/cpufreq/mediatek-cpufreq.c` | `compatible = "mediatek,cpufreq"` | Partial | Optional | Research Further | Kali/Gemian — Android/MTK vendor driver — MTK custom DVFS/EAS framework (`drivers/misc/mediatek/base/power/`) tightly coupled to Android power HAL; not portable | MediaTek CPUFreq driver in mainline but MT6797 3-cluster topology + RT5735 regulator dependency is incomplete. Run at fixed frequency initially. |
| **Vibrator** | Integrated vibrator motor | Haptic feedback | Vendor vibrator driver | `compatible = "mediatek,vibrator"` | Not Upstreamed | Optional | Defer | Kali/Gemian — Android/MTK vendor driver — Android vibrator HAL + MTK driver; not ported | Simple vibrator. Low priority. |
| **NFC** | Unknown (DTS node present) | Near Field Communication | Vendor NFC driver | `compatible = "mediatek,nfc"` | Not Upstreamed | Optional | Defer | Kali/Gemian — Android/MTK vendor driver — vendor NFC driver; may be unpopulated on hardware | NFC chip model not identified from DTS. Gemini PDA shipping specs do not advertise NFC — may be unpopulated on production hardware. |
| **Cameras** | MIPI CSI-2 (front + rear, 5 MP each) | Camera capture | Vendor camera HAL | `mediatek,camera_main` / `mediatek,camera_sub` | Not Upstreamed | Optional | Defer | Kali/Gemian — Android/MTK vendor driver — full MTK camera HAL + SENINF driver; not ported to V4L2 | Camera sensor model not identifiable from DTS. MTK MIPI CSI-2 (SENINF) partially in mainline via V4L2. Low priority. |
| **Buck-Boost** | Integrated / MT6351 | Boost converter for display bias | Vendor | `compatible = "mediatek,buck_boost"` | Not Required | Usability Critical | Stub / Disable | Kali/Gemian — Android/MTK vendor driver — controlled via MTK PMIC framework; no standalone driver | Not required. LCD bias is supplied by TPS65132 (I2C1/0x3E), not this node. `mediatek,buck_boost` is an LCM framework abstraction node — not a real hardware driver needed for mainline. Ignore. |
| **I2C LCD Bias** | TI TPS65132 (±5 V dual output) | LCD panel AVDD/AVEE bias supply | Vendor `mediatek,i2c_lcd_bias` → `tps65132_write_bytes()` | `i2c_lcd_bias@3e` on I2C1 | Not Upstreamed | Usability Critical | Use Upstream (gpio-regulator) | Kali/Gemian — Android/MTK vendor driver — TPS65132 at I2C1 (0x11008000), address 0x3E; vendor driver writes 0x0E to registers 0x00 (AVDD) and 0x01 (AVEE) to set ±5V; mainline `tps65132` regulator driver exists | **Resolved.** TPS65132 identified from vendor LCM source `r63419_wqhd_truly_phantom_2k_cmd_ok_mt6797.c`. I2C1 at address 0x3E. For Phase 5 bring-up: using `regulator-fixed` with GPIO60 (ENP) and GPIO251 (ENN) enable pins — avoids dependency on a TPS65132 I2C driver. Mainline `drivers/regulator/tps65132-regulator.c` exists and could be used for full voltage control later. Panel DTS wires `avdd-supply` and `avee-supply` to these regulators. Not blocking. |
| **RTC** | MT6351 integrated RTC | Real-time clock | PMIC RTC driver | Via PMIC wrapper | **Not Upstreamed** (CORRECTED 2026-06-10) | Optional | Defer | Kali/Gemian — Android/MTK vendor driver — RTC accessed via MTK PMIC framework in vendor 3.18 | **CORRECTION:** no mt6351 RTC driver in v6.6 (`drivers/rtc/` has no mt6351 entries; depends on the missing MT6351 MFD — see PMIC row). System can run without RTC (NTP after networking). Defer with the PMIC port. |
| **Watchdog** | MT6797 integrated watchdog | System reset watchdog | `drivers/watchdog/mtk_wdt.c` | `mediatek,mtk-wdt` | Upstreamed | Boot Critical | Use Upstream | Kali/Gemian — Android/MTK vendor driver — custom MTK watchdog in `drivers/misc/mediatek/`; mainline `mtk_wdt.c` is upstream equivalent | MTK watchdog driver in mainline. Must be handled correctly to avoid spurious resets during bring-up. |
| **TRNG** | MT6797 hardware RNG | True random number generator | `drivers/char/hw_random/mtk-rng.c` | `compatible = "mediatek,trng"` | Upstreamed | Optional | Use Upstream | Kali/Gemian — Android/MTK vendor driver — vendor MTK RNG; mainline `mtk-rng.c` is upstream equivalent | MTK RNG driver in mainline. |
| **Preloader / LK** | MediaTek preloader + LittleKernel | First-stage bootloader chain | Proprietary | `preloader_k97v1_64_bsp.bin` / `lk_s.img` | Not Applicable | Boot Critical | Not Required | Not Applicable — proprietary binaries, unchanged across all projects | Vendor bootloader chain unchanged. LK boots the Linux kernel from `boot2` partition. Not modified in this project. |
| **TrustZone / TEE** | MediaTek TEE (Trustonic Mobicore + Microtrust UTOS) | Secure world execution | Proprietary | `tee1` / `tee2` partitions; `trustonic,mobicore` / `microtrust,utos` | Not Applicable | Boot Critical | Not Required | Not Applicable — proprietary binaries, unchanged across all projects | Required for secure boot. Run as-is from vendor images. Do not modify. |

---

## Summary by Tier

### Boot Critical

| Subsystem | Mainline Status | Reference Driver | Action |
|-----------|----------------|-----------------|--------|
| CPU (MT6797X) | Upstreamed | Android/MTK vendor | Use Upstream |
| GIC-v3 | Upstreamed | Upstream | Use Upstream |
| ARMv8 Timer | Upstreamed | Upstream | Use Upstream |
| Clocks (TOPCKGEN / APMIXED) | Upstreamed | Android/MTK vendor | Use Upstream |
| Pinctrl / GPIO | Upstreamed | Android/MTK vendor | Use Upstream |
| PMIC MT6351 (via PWRAP) | **Not Upstreamed** (corrected 2026-06-10) | Android/MTK vendor | Fixed-regulator stubs for bring-up; port later |
| PMIC Wrapper | Upstreamed | Android/MTK vendor | Use Upstream |
| RT5735 Buck Regulators | Not Upstreamed | Android/MTK vendor | Port Driver — **code complete, compiles** — [details](driver_ports.md#rt5735-buck-regulator) |
| eMMC (MSDC) | Upstreamed | Android/MTK vendor | Use Upstream |
| UART (ttyMT0) | Upstreamed | Android/MTK vendor | Use Upstream |
| Thermal | Upstreamed | Android/MTK vendor | Use Upstream |
| Watchdog | Upstreamed | Android/MTK vendor | Use Upstream |
| TrustZone / Preloader | Not Applicable | Not Applicable | Not Required |

### Usability Critical

| Subsystem | Mainline Status | Reference Driver | Action |
|-----------|----------------|-----------------|--------|
| Display Controller (MMSYS/DDP) | Partial | Android/MTK vendor | Port Driver — **code complete, compiles** — [details](driver_ports.md#mt6797-display-controller) |
| Display Panel (R63419) | Not Upstreamed | Android/MTK vendor | Port Driver — **code complete, compiles** — [details](driver_ports.md#r63419-panel) |
| Touchscreen (Solomon SSD2092) | Not Upstreamed | Downstream Linux (Gemian SSD20xx) | Port Driver — **WORKING ON HARDWARE 2026-07-19** (build #266, multi-touch events verified; `patches/v6.6/input/0002`, polled until EINT/B-11) |
| Keyboard Matrix (AW9523B) | Not Upstreamed | Downstream Linux (Gemian) | Port Driver — **WORKING ON HARDWARE 2026-07-12** (typing at panel console, build #164/#168; polled via `patches/v6.6/input/0001` until EINT/B-11; Fn layer + B-18 USB regression outstanding) — [details](driver_ports.md#aw9523b-keyboard) |
| USB left port (mtu3, peripheral+charging) | Upstreamed | Android/MTK vendor | Use Upstream — **WORKING 2026-07-16** (build #255, B-22) |
| USB right port (MUSB, host) | Upstreamed (Gemini config variant) | Android/MTK vendor | Use Upstream + local config — **WORKING 2026-07-16** (build #255, B-22) |
| USB-C CC (FUSB301A ×2) | Not Upstreamed | Android/MTK vendor | Port Driver — **code complete, compiles; I2C0 (right) + I2C1 (left) 0x25 confirmed** — [details](driver_ports.md#fusb301a-cc-controller) |
| Wi-Fi / BT (MT6797 CONSYS) | Not Upstreamed | Android/MTK vendor + Re4son patches | Port Driver — deferred to Phase 9; USB-Ethernet for Phase 8 — [details](driver_ports.md#mt6797-consys-wifibt) |
| Battery Charger (BQ25896 — corrected 2026-07-14, was misidentified as RT9466) | Upstreamed | Android/MTK vendor | Use Upstream |
| Fuel Gauge (MT6351) | Partial | Android/MTK vendor | Research Further |
| Display Bias / Buck-Boost | Unknown | Android/MTK vendor | Research Further |

### Optional

| Subsystem | Mainline Status | Reference Driver | Action |
|-----------|----------------|-----------------|--------|
| GPU (Mali T860 / Panfrost) | Upstreamed | Android/MTK vendor (proprietary DDK) | Use Upstream |
| Audio (MT6351 ASoC) | Upstreamed | Android/MTK vendor | Use Upstream |
| HDMI (SII9022A) | Upstreamed | Android/MTK vendor | Use Upstream |
| Accelerometer (BMI160 / LSM6DS3) | Upstreamed | Android/MTK vendor (crash risk) | Use Upstream |
| Gyroscope (BMI160 / LSM6DS3) | Upstreamed | Android/MTK vendor (crash risk) | Use Upstream |
| Barometer (BMP280) | Upstreamed | Android/MTK vendor | Use Upstream |
| Humidity (HTS221) | Upstreamed | Android/MTK vendor | Use Upstream |
| Magnetometer (MMC3530) | Not Upstreamed | Android/MTK vendor | Defer |
| ALS / Proximity (STK3x1x) | Not Upstreamed | Android/MTK vendor (crash risk) | Defer |
| Fingerprint (FPC1145) | Not Upstreamed | Android/MTK vendor | Defer |
| Keyboard Backlight (AW9120) | Not Upstreamed | Android/MTK vendor | Port Driver |
| SCP Sensor Hub | Partial | Android/MTK vendor | Defer |
| CPUFreq / DVFS | Partial | Android/MTK vendor | Research Further |
| LTE Modem (md1) | Not Upstreamed | Android/MTK vendor | Defer |
| Cameras | Not Upstreamed | Android/MTK vendor | Defer |
| NFC | Not Upstreamed | Android/MTK vendor | Defer |
| Vibrator | Not Upstreamed | Android/MTK vendor | Defer |
| TRNG | Upstreamed | Android/MTK vendor | Use Upstream |
| RTC | Upstreamed | Android/MTK vendor | Use Upstream |

---

## Key Findings

1. **Nearly every subsystem in Kali and Gemian uses an Android/MTK vendor driver.** Both projects run the 3.18 MediaTek BSP kernel with minimal Linux-specific changes. The only subsystem with a genuine downstream Linux driver is the **keyboard matrix** (Gemian), which is the best porting reference in the project.

2. **Core boot stack is well covered upstream.** CPU, GIC, timers, clocks, pinctrl, PMIC wrapper, eMMC (MSDC), UART, thermal, and watchdog all have mainline drivers that are clean rewrites of the vendor originals. Do not port from 3.18 — use mainline directly.

3. **RT5735 buck regulators are the main boot-critical gap.** These supply VPROC (CPU voltage) and VGPU. Without a working regulator driver the kernel cannot perform DVFS. For initial bring-up: run at a fixed voltage (skip regulator registration) or port the driver. Check register compatibility with mainlined Richtek parts first.

4. **Wi-Fi/BT is deferred to Phase 9.** MT6797 CONSYS (integrated Wi-Fi/BT/GPS) is entirely proprietary. Architecture confirmed: WiFi uses AHB bus (not SDIO), BT uses BTIF/STP. conninfra, mt76, and btmtksdio are all inapplicable. The vendor driver stack is ~75–103 KLOC and requires a userspace daemon (`wmt_launcher`). The last known working out-of-tree port (frank-w BPI-Router-Linux) ran on kernel 5.6 and is broken on 6.0+. USB-Ethernet (USB dongle via `xhci-mtk`) covers Phase 8 networking. See [research.md](research.md) for full findings.

5. **Display requires significant work and MT6797 has no upstream DRM path.** The mainline MTK DRM driver supports mt2701/mt8173/mt8183/mt8186/mt8195 — MT6797 is absent from every component. MMSYS clocks exist in `clk-mt6797-mm.c`. At minimum, four files require new MT6797 variants: DDP topology (`mtk_drm_drv.c`), MIPITX PHY (`phy-mtk-mipi-dsi.c`), DSI controller (`mtk_dsi.c`), and display pipeline DTS nodes in `mt6797.dtsi`. The R63419 panel init sequence is fully documented in vendor DTS/LCM source. Display bias IC identity is unknown and needs hardware investigation before display bring-up can proceed.

6. **AW9523B GPIO driver is NOT in mainline** — the prior note claiming "mainlined ~6.0" is incorrect (no `aw9523` entries found in `drivers/gpio/` in Linux 6.6). This changes the keyboard plan: `matrix_keypad.c` exists and uses standard gpiolib, but requires an AW9523B GPIO driver first. Two paths: write AW9523B GPIO driver + use `matrix_keypad` (clean, upstreamable), or port Gemian's custom input driver directly (most direct). Gemian's driver remains the best reference implementation.

7. **Sensor driver crash risk.** STK3x1x, LSM6DS3, and BMI160 vendor drivers caused crash loops on prior 3.18 Linux boot (I2C probe before hardware ready). On mainline with DT-driven probing this should not recur, but these drivers should be modules or excluded until the system is stable. Use IIO drivers, not vendor sensor hub.

8. ~~RT9466 charger uses `richtek,rt9467` binding.~~ **CORRECTION (2026-07-14): the charger is a TI BQ25896 at i2c0 0x6b, not an RT9466** (verified live via chip-ID register; nothing at 0x53 on any bus — the RT9466 identification came from a dead vendor config branch). Use mainline `bq25890_charger.c` with `compatible = "ti,bq25896"`. Still the simplest use-upstream path; also provides the `usb-otg-vbus` boost regulator for USB host mode. See research.md §8.

9. **USB2 host mode works without FUSB301A.** Basic USB2 (D+/D-, VBUS) does not require the CC controller. Fixed polarity and 500 mA default, but USB networking adapters and host-mode peripherals function. FUSB301A can be deferred to Phase 4+ for a minimal CC-detection driver; full Power Delivery support is not needed.

10. **MT6351 fuel gauge has no mainline support.** No MT6351 ADC or coulomb counter driver exists in Linux 6.6. Use charger-only mode (bq25890_charger.c driver — see charger correction in note 8) plus userspace voltage monitoring for minimum safe operation. Full fuel gauge requires a new `mt6351-adc.c` driver; defer to Phase 7.

11. **CONSYS Wi-Fi porting starting point is kernel 5.6, not 3.18.** The frank-w BPI-Router-Linux project already ported `wlan_drv_gen2` to kernel 5.6 (last known working). The breakage at 5.7 (BT) and 6.0 (WiFi) is the actual gap to close — not a full 3.18→6.6 leap. Starting from the frank-w 5.6 tree and identifying the 5.7/6.0 API breakages is the correct approach if this is ever attempted. Note: BPI-R2 uses external SDIO MT6625 (different HIF than MT6797 AHB), so the wifi gen2 driver core is shared but the HIF layer would need MT6797 AHB path work. USB-Ethernet remains the Phase 8 plan.

12. **Recommended minimum kernel version: 6.1 LTS** — mature Panfrost T860, MT6797 DTS support, and the bq25890 charger driver (BQ25896 support; charger identity corrected 2026-07-14, note 8). AW9523B GPIO driver is not in mainline; keyboard port is required. ~~covers MT6351 regulator (6.2)~~ **CORRECTION (2026-06-10): the MT6351 regulator never landed in mainline at any version** — verified against the v6.6 tree (only the ASoC codec exists). See PMIC row.

13. **MT6351 PMIC is NOT upstreamed (correction, 2026-06-10).** Earlier rows claimed the regulator and RTC were mainline; direct inspection of v6.6 shows no MT6351 MFD/regulator/RTC anywhere — only `sound/soc/codecs/mt6351.c`. Bring-up consequence: all MT6351 rails must be `regulator-fixed` stubs (LK leaves them configured); eMMC `vmmc`/`vqmmc` stubs needed for Phase 4; RTC deferred. A real MT6351 MFD+regulator port (mt6397-family pattern) is a new post-freeze driver-port item.

---

## Device Tree Starting Point

The vendor DTS (`compatible = "mediatek,MT6797"`) provides the hardware register map. The mainline DTS file `arch/arm64/boot/dts/mediatek/mt6797.dtsi` provides the upstream-compatible node structure. The bring-up DTS should be built from the mainline dtsi, adding Gemini-specific nodes (board file) for the peripherals confirmed above.

Vendor DTB decompiled to [`docs/vendor-dtb/gemini_kali_boot.dts`](docs/vendor-dtb/gemini_kali_boot.dts) (6098 lines, committed 2026-06-10; extracted from `planet/kali_boot.img`) — the reference for DTS authoring. The raw DTB and the known-good kernel's extracted config are alongside it.
