# Phase 5 display bring-up.
#
# DISABLED 2026-07-05 (renamed so build.sh's *.config glob skips it):
# with COMMON_CLK_MT6797_MMSYS=y the kernel hard-hangs silently at ~0.52s,
# immediately after the last DSI/DRM activity — B-13's broken MM power domain
# (mtk-scpsys MT6797 domain table) wedges the bus on first MM-domain register
# access once the mm clk driver exists. Identical final line/timestamp in
# logs/2026-07-05-21-mmsys-fix-boot.log and -23-msdc0-hclk-fix-boot.log.
# Rename back to *.config only after B-13 is fixed.
#
# RE-ENABLED 2026-07-06: scpsys probe-abort bug fixed —
# patches/v6.6/pmdomain/0001-pmdomain-mediatek-skip-unpopulated-mt6797-domain-slots.patch
# makes mtk-scpsys.c skip the mt6797 domain table's unpopulated MFG/MFG_CORE0-3
# slots instead of treating their NULL regulator id as fatal for the whole
# provider. This is blockers.md B-13's recommended "safe workaround" fix path
# (option 2): no GPU register values needed, MM domain (and everything sharing
# its platform device) now probes normally.
#
# UPDATE 2026-07-06 (BUILD #79): hardware-tested with this fragment enabled.
# The fix is necessary but NOT sufficient — boot progresses further
# (mediatek-drm component matching, panel-renesas-r63419 registers) but then
# hard-hangs and watchdog-reboot-loops, presumed in MM-domain power-on
# register access itself. B-13 remains OPEN; see blockers.md. This fragment
# is left enabled in the repo since the scpsys fix is real forward progress,
# but flashing a build with it is NOT currently safe — do not leave it
# flashed on the device without further work.
#
# The Gemini's initramfs is the vendor 2019 Kali ramdisk with rdinit=/bin/sh
# (see gemini-cmdline.config) -- there is no modprobe/module path available
# in that minimal shell environment. Every driver in the display chain must
# therefore be built in (=y), not =m, or the panel will never light up
# regardless of whether the DT nodes are correct.
#
# defconfig ships DRM, DRM_KMS_HELPER, DRM_MEDIATEK and PHY_MTK_MIPI_DSI as
# modules, and does not enable DRM_PANEL_RENESAS_R63419 at all. Force all of
# them built-in here.
CONFIG_MTK_CMDQ=y
CONFIG_MTK_CMDQ_MBOX=y
CONFIG_MTK_MMSYS=y

# CONFIG_COMMON_CLK_MT6797_MMSYS is a tristate with no "default" in Kconfig,
# so defconfig leaves it (and its IMGSYS/VDECSYS/VENCSYS siblings) off.
# mtk-mmsys.c unconditionally registers a "clk-mt6797-mm" child platform
# device on probe; without this driver built in, that device has no driver
# to bind to, so CLK_MM_DSI0_MM_CLOCK (and every other mm-domain gate clock)
# never registers and the DSI "engine" clk consumer defers -EPROBE_DEFER
# forever. Built-in (=y), not =m, for the same rdinit=/bin/sh reason as the
# rest of this fragment.
CONFIG_COMMON_CLK_MT6797_MMSYS=y
CONFIG_COMMON_CLK_MT6797_IMGSYS=y
CONFIG_COMMON_CLK_MT6797_VDECSYS=y
CONFIG_COMMON_CLK_MT6797_VENCSYS=y

CONFIG_DRM=y
CONFIG_DRM_KMS_HELPER=y
CONFIG_DRM_MEDIATEK=y
CONFIG_PHY_MTK_MIPI_DSI=y
# The Gemini's FHD 1080x2160 video-mode panel. Two variants shipped on
# identical wiring — a Novatek NT36672 (this unit) and a Solomon SSD2092 —
# and the driver picks between them at runtime from the LCM name LK publishes
# in /chosen/atag,videolfb, so one build serves both.
#
# Renamed 2026-08-21 from DRM_PANEL_SOLOMON_SSD2092. The old name was wrong on
# this unit (the fitted panel is the Novatek) and wrong in principle now that
# the driver serves both. The R63419 WQHD assumption that preceded even that
# came from the vendor DTB's build-time videolfb template string, before
# anyone read the one LK overwrites it with (boot.md build #149).
CONFIG_DRM_PANEL_GEMINI_FHD=y
CONFIG_BACKLIGHT_CLASS_DEVICE=y

# Backlight: DISP_PWM0 @ 0x1100f000 -> GPIO178 -> LED boost on the display
# flex (vendor led_mode=<5> CUST_BLS_PWM). pwm-mtk-disp gets a mt6797 compat
# (patches/v6.6/pwm/0001) reusing the mt8173 register data — layout verified
# against vendor ddp_reg.h and live devmem on hardware (build #151 debug).
CONFIG_PWM=y
CONFIG_PWM_MTK_DISP=y
CONFIG_BACKLIGHT_PWM=y

# TPS65132 LCD bias (±5.4 V AVDD/AVEE for the SSD2092 panel, I2C1 @0x3e).
# VPOS/VNEG registers are volatile; LK reprograms them every boot. Without
# this driver the panel booster stays off → backlit-black (capture 250).
CONFIG_REGULATOR_TPS65132=y

# ADDED 2026-07-12: large console font for the 403-dpi 1080x2160 panel —
# the default 8x16 font is unreadable on glass. Selected on the cmdline
# with fbcon=font:TER16x32 (gemini-cmdline.config). Rotation is also set
# there (fbcon=rotate:1, panel native-portrait, device used landscape).
CONFIG_FONT_SUPPORT=y
CONFIG_FONTS=y
CONFIG_FONT_TER16x32=y
CONFIG_FRAMEBUFFER_CONSOLE_ROTATION=y
