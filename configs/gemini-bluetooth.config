# Bluetooth over the CONSYS STP mux (gemian drv_bt hci_stp route — a native
# BlueZ hci0, no chardev/vhci bridge). BT=m/BT_HIDP come from the base
# config; these add BLE (HOG mice need uhid + LE) and the vhci fallback.
CONFIG_BT_LE=y
CONFIG_UHID=m
CONFIG_BT_HCIVHCI=m
