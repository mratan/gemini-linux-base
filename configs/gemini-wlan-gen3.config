# Gen3 WLAN driver (tracker issue #10, Slice 9) build requirements.
# The vendor gen3 driver hard-#errors without cfg80211 testmode support
# (gl_p2p_ioctl.h) and drives cfg80211 directly.
CONFIG_CFG80211=m
CONFIG_NL80211_TESTMODE=y
CONFIG_CFG80211_WEXT=y
