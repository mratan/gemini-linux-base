# macOS
.DS_Store

# Flash tool state (generated by mtkclient)
hwparam.json
.state

# Large binary images — store these outside git
*.img
*.bin
*.zip
zips/
kali/
Gemini_x25_x27_06052019/
planet/

#archive files
archive/

# Flash tool binaries and runtime state
FlashToolLinux/*.so
FlashToolLinux/*.so.*
FlashToolLinux/flash_tool
FlashToolLinux/BackupData/
FlashToolLinux/history.ini

# Claude local settings
.claude/settings.local.json
docs/firmware-consys/

# capture-kit (issue #9): staging holds the Gemian ramdisk + packed boot
# image (firmware-adjacent blobs, never committed)
capture-kit/staging/
